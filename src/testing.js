// type rafce + enter (ES7 snippets extension)

import React from 'react';

const testing = () => {
  return <div></div>;
};

export default testing;

// testing is lower case because we're exporting, once we use it, it will have to be upper case
// components go with capital case
